<%= form_with(model: detalle_ordene, local: true) do |form| %>
  <% if detalle_ordene.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(detalle_ordene.errors.count, "error") %> prohibited this detalle_ordene from being saved:</h2>

      <ul>
      <% detalle_ordene.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cantidad %>
    <%= form.number_field :cantidad %>
  </div>

  <div class="field">
    <%= form.label :precio_venta %>
    <%= form.text_field :precio_venta %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
