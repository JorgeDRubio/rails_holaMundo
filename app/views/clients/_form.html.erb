<%= form_with(model: client, local: true) do |form| %>
  <% if client.errors.any? %>
    <div id="error_explanation">
      <h2 class="display-4"><%= pluralize(client.errors.count, "error") %> prohibited this cliente from being saved:</h2>

      <ul class="list-unstyled">
      <% client.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :rfc %>
    <%= form.text_field :rfc %>
  </div>

  <div class="field">
    <%= form.label :nombre_cliente %>
    <%= form.text_field :nombre_cliente %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>